[project]
name = "pd-masker"
version = "1.0.0"
description = "PII masking service"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]